name: Deploy React app to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  build:
    name: Build React app
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: react-metronome/package-lock.json

      - name: Install dependencies
        working-directory: react-metronome
        run: npm ci --loglevel=error

      - name: Build static app
        working-directory: react-metronome
        run: npm run build

      - name: Add SPA fallback
        run: cp react-metronome/dist/index.html react-metronome/dist/404.html

      - name: Verify build output
        run: |
          test -f react-metronome/dist/index.html
          test -f react-metronome/dist/404.html
          ls -la react-metronome/dist

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: react-metronome/dist

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy artifact
        id: deployment
        uses: actions/deploy-pages@v5
